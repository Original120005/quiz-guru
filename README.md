<div align="center">

# Quiz Guru

**Интерактивная платформа с квизами, рейтингами и генерацией вопросов через ИИ (Grok)**

[![License: MIT](https://img.shields.io/badge/License-MIT-yellow.svg)](https://opensource.org/licenses/MIT)
[![PRs Welcome](https://img.shields.io/badge/PRs-welcome-brightgreen.svg)](http://makeapullrequest.com)

</div>

---

## Описание

Quiz Guru — это интерактивная веб‑платформа для создания и прохождения квизов. Пользователи могут регистрироваться, проходить квизы по темам, зарабатывать баллы и просматривать статистику. Платформа поддерживает генерацию вопросов с помощью интеграции с ИИ (Grok) и содержит адаптивный интерфейс.

---

## Особенности

* Регистрация и вход (JWT + HTTP‑only cookies)
* Личный профиль: статистика, аватар, история ответов
* Квизы по темам: Игры, Спорт, География, Животные (расширяемо)
* Поиск квизов и фильтры по категориям/сложности
* Система очков за правильные ответы и лидерборд
* Генерация квизов через ИИ (Grok)
* Адаптивный дизайн (SCSS)
* Тесты и seed‑скрипты для заполнения БД тестовыми данными

---

## Технологии

| Слой           | Технология                     |
| -------------- | ------------------------------ |
| Frontend       | Next.js + TypeScript + SCSS    |
| Backend        | Node.js + Express + TypeScript |
| База данных    | MySQL + Prisma                 |
| Аутентификация | JWT + HTTP-only cookies        |

---

## Структура репозитория (пример)

```
quiz-guru/
├─ client/        # Next.js frontend
├─ server/        # Express backend (TypeScript)
│  ├─ prisma/     # Prisma schema & seeds
│  ├─ src/
│  │  ├─ controllers/
│  │  ├─ routes/
│  │  ├─ services/
│  │  └─ middleware/
│  └─ package.json
├─ .gitignore
└─ README.md
```

---

## Быстрый старт — локальная разработка

> Ниже — пошаговая инструкция для запуска проекта локально.

### 1) Клонировать и установить зависимости

```bash
git clone https://github.com/username/quiz-guru.git
cd quiz-guru

# Установить зависимости для сервера
cd server
npm install

# Установить зависимости для клиента
cd ../client
npm install
```

### 2) Настройка базы данных

```bash
# Создать базу данных (пример для MySQL)
mysql -u root -p -e "CREATE DATABASE quizguru;"

# Перейти в сервер
cd server

# Скопировать пример env и отредактировать
cp .env.example .env
# Отредактируйте server/.env:
# DATABASE_URL="mysql://root:ПАРОЛЬ@localhost:3306/quizguru"
# JWT_SECRET="твой-секретный-ключ"
# XAI_API_KEY="твой-grok-api-ключ"
# PORT=5000

# Сгенерировать Prisma Client и применить миграции
npx prisma generate
npx prisma migrate dev --name init
```

### 3) Добавление тестовых данных (seeds)

```bash
# Пример: добавить квизы для категорий
cd server
npx ts-node prisma/seeds/games-quizzes.ts
npx ts-node prisma/seeds/sport-quizzes.ts
npx ts-node prisma/seeds/geography-quizzes.ts
npx ts-node prisma/seeds/animals-quizzes.ts
```

> Если используете Windows и возникают проблемы с `ts-node`, можно запустить собранный скрипт через `node` после транспиляции или использовать `npm run seed` (если в package.json настроен скрипт).

### 4) Запуск приложения в режиме разработки

```bash
# Запустить сервер (порт по умолчанию 5000)
cd server
npm run dev

# В отдельном терминале — клиент (порт по умолчанию 3001)
cd client
npm run dev
```

### 5) Открыть приложение

Откройте в браузере: `http://localhost:3001`

---

## Дополнительные команды

### Работа с базой данных (Prisma)

```bash
# Открыть Prisma Studio (визуальный редактор БД)
cd server
npx prisma studio

# Создать новую миграцию
npx prisma migrate dev --name описание_изменения

# Сбросить базу данных и примеси
npx prisma migrate reset
```

### Производственный запуск (build & production)

```bash
# Сборка клиента
cd client
npm run build
npm start

# Запуск продакшн сервера
cd server
npm start
```

---

## API — основные эндпоинты

| Метод | Путь                            | Описание                                |
| ----- | ------------------------------- | --------------------------------------- |
| POST  | `/api/auth/register`            | Регистрация пользователя                |
| POST  | `/api/auth/login`               | Вход в систему (получение JWT / cookie) |
| GET   | `/api/user/me`                  | Получить профиль текущего пользователя  |
| GET   | `/api/categories`               | Список всех категорий                   |
| GET   | `/api/categories/:slug/quizzes` | Квизы по категории                      |
| GET   | `/api/quiz/:id`                 | Получить квиз по ID                     |
| POST  | `/api/quiz/generate`            | Сгенерировать квиз через AI (Grok)      |

> Для продвинутых возможностей (создание/редактирование/удаление квизов, модерация, загрузка медиа) добавьте соответствующие маршруты и права доступа.

---

## Пример структуры вопроса квиза (TypeScript)

```ts
type QuizQuestion = {
  question: string;
  options: string[]; // длина: 2-6
  correct: number; // индекс правильного варианта (0..n-1)
  explanation?: string; // необязательно: объяснение ответа
  difficulty?: 'easy' | 'medium' | 'hard';
};

const example: QuizQuestion = {
  question: "Какой звук производит собака?",
  options: ["Мяу", "Гав", "Кря"],
  correct: 1,
};
```

---

## Переменные окружения

Файл: `server/.env`

```
DATABASE_URL="mysql://root:password@localhost:3306/quizguru"
JWT_SECRET="your-jwt-secret"
XAI_API_KEY="your-grok-api-key"
PORT=5000
```

Файл: `client/.env.local` (или `client/.env`)

```
NEXT_PUBLIC_API_URL=http://localhost:5000/api
```

---

## Для разработчиков — добавление новой категории

1. Добавьте модель/отношения в `server/prisma/schema.prisma` (если требуется).
2. Создайте seed‑файл в `server/prisma/seeds/` для заполнения квизов новой категории.
3. Запустите seed‑скрипт (пример `npx ts-node prisma/seeds/your-category-quizzes.ts`).
4. Обновите фронтенд: добавьте новую карточку категории и роуты при необходимости.

---

## Лицензия

Этот проект распространяется под лицензией MIT. Подробнее — в файле `LICENSE`.

---

<div align="center">
⭐ Если проект понравился — поставь звезду!  
Happy quizzing! 🎉

</div>
